openapi: 3.2.0
info:
  title: Reference Subpackage Versions API
  version: 1.0.0
servers:
- url: https://api.x.flatfile.com/v1
tags:
- name: subpackage_versions
paths:
  /versions:
    post:
      operationId: create-id
      summary: Create Id
      tags:
      - subpackage_versions
      parameters:
      - name: Authorization
        in: header
        description: Bearer authentication
        required: true
        schema:
          type: string
      - name: X-Disable-Hooks
        in: header
        required: true
        schema:
          type: string
          enum:
          - 'true'
      responses:
        '200':
          description: Response with status 200
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/type_versions_VersionResponse'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                sheetId:
                  $ref: '#/components/schemas/type_commons_SheetId'
                  description: The ID of the Sheet.
                parentVersionId:
                  $ref: '#/components/schemas/type_commons_VersionId'
                  description: Deprecated, creating or updating a group of records together will automatically generate a commitId to group those record changes together.
components:
  schemas:
    type_versions_Version:
      type: object
      properties:
        versionId:
          $ref: '#/components/schemas/type_commons_VersionId'
      required:
      - versionId
      title: Version
    type_commons_VersionId:
      type: string
      description: Version ID
      title: VersionId
    type_commons_SheetId:
      type: string
      description: Sheet ID
      title: SheetId
    type_versions_VersionResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/type_versions_Version'
      required:
      - data
      title: VersionResponse
  securitySchemes:
    default:
      type: http
      scheme: bearer
